import Layout from '@/layout/index';
import Works from 'components/subpages/Works';

const WorksPage = () => {
  return (
    <Layout>
      <Works />
    </Layout>
  );
};

export default WorksPage;
